export enum SectionType {
    BIG = 'big',
    MEDIUM = 'medium',
    SMALL = 'small',
    INFO = 'info',
}

export enum UserType {
    PROVIDER = 'provider',
    CUSTOMER = 'customer',
}

export enum PriceUnit {
    ABSOLUTE = 'absolute',
    PERSON = 'person',
    STACK = 'stack',
}
